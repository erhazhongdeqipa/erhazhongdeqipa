language: node_js
node_js: stable

# S: Build Lifecycle
install:
  - npm install

#before_script:
 # - npm install -g gulp

script:
  - hexo clean
  - hexo g

after_script:
  - cd ./public
  - git init
  - git config user.name "erhazhongdeqipa" --{GitHub账户名称}
  - git config user.email "1509573865@qq.com" --{Github账户邮箱}
  - git add .
  - git commit -m "Update docs"
  - git push --force --quiet "https://${GH_TOKEN}@${GH_REF}" master:master
# E: Build LifeCycle

branches:
  only:
    - master --{Hexo源码分支名称}
env:
 global:
   - GH_REF: git@github.com:erhazhongdeqipa/erhazhongdeqipa.git --{仓库地址}

# .travis.yml 配置
cache:
  directories:
    - node_modules